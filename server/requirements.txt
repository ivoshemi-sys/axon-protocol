fastapi==0.115.0
uvicorn==0.30.6
aiofiles>=23.0.0
pydantic==2.8.2
aiosqlite==0.20.0
asyncpg==0.29.0
websockets==13.0
python-telegram-bot==21.5
httpx==0.27.2
python-dotenv==1.0.1
cryptography==43.0.1
rich==13.8.1
# Blockchain
web3==6.20.3
py-solc-x==2.0.4
# Arbiter
anthropic>=0.40.0
# Stripe payments
stripe>=8.0.0
# MCP (Model Context Protocol) — for Claude Desktop / Cursor integration
# Optional: server starts without it, but /mcp/sse and stdio mode require it
mcp>=1.0.0
# AgentOps — agent observability and monitoring
agentops>=0.3.0
